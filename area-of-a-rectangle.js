function findArea( length, width ) {
    const recArea = length * width;
    return recArea;
    
}

const recLength = 10;
const recWidth = 5;
const result = findArea(recLength, recWidth);
console.log(result);